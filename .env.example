# 数据存储基础路径配置
# Base path for data storage
#
# 本地开发环境:
# - 如果不设置此变量，默认使用项目根目录下的 data 路径
# - 示例: BASE_URL=/path/to/custom/data
#
# 生产环境:
# - 由部署脚本自动从 GitHub Secrets 读取并写入
# - 配置位置: GitHub Repository Settings -> Secrets -> BASE_URL
#
BASE_URL=/opt/diting/data

# DeepSeek API Key for LLM message analysis
# Get your API key from: https://platform.deepseek.com/
DEEPSEEK_API_KEY=your_api_key_here
