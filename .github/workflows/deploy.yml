name: Deploy to Aliyun ECS

# 触发条件: 等待 Test workflow 成功后执行
# 使用 workflow_run 确保只有测试通过的代码才会部署
on:
  workflow_run:
    workflows: ["Test"]  # 等待 Test workflow 完成
    types: [completed]
    branches: [master]

# 权限配置
permissions:
  contents: read
  issues: write  # 用于创建失败通知 Issue

# 串行执行部署(防止并发冲突)
concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  deploy:
    # 安全检查: 仅在 Test workflow 成功时运行
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15  # 超时保护
    # environment:  # 暂时注释，使用 Repository Secrets
    #   name: production
    #   url: http://${{ secrets.ALIYUN_ECS_HOST }}:8000

    steps:
      # Step 1: 检出代码
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: 配置 SSH 密钥
      - name: Setup SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.ALIYUN_SSH_PRIVATE_KEY }}

      # Step 3: 添加 ECS 到 known_hosts(防止中间人攻击)
      - name: Add ECS to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.ALIYUN_ECS_HOST }} >> ~/.ssh/known_hosts

      # Step 4: 创建部署版本目录
      - name: Create release directory
        id: create-release
        run: |
          RELEASE_ID=$(date +%s)
          RELEASE_DIR="/opt/diting/releases/$RELEASE_ID"
          echo "RELEASE_ID=$RELEASE_ID" >> $GITHUB_OUTPUT
          echo "RELEASE_DIR=$RELEASE_DIR" >> $GITHUB_OUTPUT

          ssh ${{ secrets.ALIYUN_SSH_USER }}@${{ secrets.ALIYUN_ECS_HOST }} \
            "mkdir -p $RELEASE_DIR"

          echo "✅ 创建版本目录: $RELEASE_DIR"

      # Step 5: 上传代码到 ECS
      - name: Upload code to ECS
        run: |
          echo "::group::上传代码到 ECS"
          rsync -avz \
            --exclude='.git' \
            --exclude='.venv' \
            --exclude='__pycache__' \
            --exclude='*.pyc' \
            --exclude='.pytest_cache' \
            --exclude='.ruff_cache' \
            --exclude='.mypy_cache' \
            --exclude='coverage.xml' \
            --exclude='.coverage' \
            --exclude='logs/' \
            ./ ${{ secrets.ALIYUN_SSH_USER }}@${{ secrets.ALIYUN_ECS_HOST}}:${{ steps.create-release.outputs.RELEASE_DIR }}/

          echo "✅ 代码上传完成"
          echo "::endgroup::"

      # Step 6: 安装依赖
      - name: Install dependencies on ECS
        run: |
          echo "::group::安装依赖"
          ssh ${{ secrets.ALIYUN_SSH_USER }}@${{ secrets.ALIYUN_ECS_HOST }} << 'EOF'
            cd ${{ steps.create-release.outputs.RELEASE_DIR }}

            # 安装依赖
            /home/${{ secrets.ALIYUN_SSH_USER }}/.local/bin/uv sync --frozen

            echo "✅ 依赖安装完成"
          EOF
          echo "::endgroup::"

      # Step 7: 更新 systemd 服务文件
      - name: Update systemd service file
        run: |
          ssh ${{ secrets.ALIYUN_SSH_USER }}@${{ secrets.ALIYUN_ECS_HOST }} << 'EOF'
            # 复制服务文件到系统目录
            sudo cp ${{ steps.create-release.outputs.RELEASE_DIR }}/deploy/diting.service /etc/systemd/system/diting.service

            # 重新加载 systemd 配置
            sudo systemctl daemon-reload

            echo "✅ Systemd 服务文件已更新"
          EOF

      # Step 8: 原子性激活新版本
      - name: Activate new release
        run: |
          ssh ${{ secrets.ALIYUN_SSH_USER }}@${{ secrets.ALIYUN_ECS_HOST }} << 'EOF'
            # 保存当前版本为 previous(用于回滚)
            if [ -L /opt/diting/current ]; then
              CURRENT_TARGET=$(readlink /opt/diting/current)
              ln -sfn "$CURRENT_TARGET" /opt/diting/previous
            fi

            # 切换到新版本
            ln -sfn ${{ steps.create-release.outputs.RELEASE_DIR }} /opt/diting/current

            echo "✅ 新版本已激活"
          EOF

      # Step 9: 重启服务
      - name: Restart service
        run: |
          ssh ${{ secrets.ALIYUN_SSH_USER }}@${{ secrets.ALIYUN_ECS_HOST }} \
            "sudo systemctl restart diting"

          echo "✅ 服务已重启"

      # Step 10: 健康检查
      - name: Health check
        id: health-check
        run: |
          ssh ${{ secrets.ALIYUN_SSH_USER }}@${{ secrets.ALIYUN_ECS_HOST }} << 'EOF'
            # 等待 HTTP 健康检查通过(最多 60 秒)
            echo "🔍 开始健康检查 http://localhost:17999/health"

            for i in {1..60}; do
              if curl -f http://localhost:17999/health > /dev/null 2>&1; then
                echo "✅ 健康检查通过 ($i/60)"

                # 验证 JSON 响应格式
                HEALTH_RESPONSE=$(curl -s http://localhost:17999/health)
                if echo "$HEALTH_RESPONSE" | jq -e '.status == "healthy"' > /dev/null 2>&1; then
                  echo "✅ 健康检查响应格式正确: $HEALTH_RESPONSE"

                  # 验证 systemd 服务状态(仅用于日志,不影响部署)
                  if sudo systemctl is-active --quiet diting; then
                    echo "✅ Systemd 服务状态: active"
                  else
                    echo "⚠️  Systemd 服务状态: $(sudo systemctl is-active diting 2>&1 || echo 'unknown')"
                  fi

                  exit 0
                fi
              fi

              # 每 10 秒输出一次详细诊断信息
              if [ $((i % 10)) -eq 0 ]; then
                echo "⏳ 等待健康检查... ($i/60)"
                echo "   端口检查: $(ss -tlnp | grep 17999 || echo '端口未监听')"
                echo "   进程检查: $(pgrep -f uvicorn || echo '进程未运行')"
              fi

              sleep 1
            done

            echo "❌ 健康检查失败 - 60 秒超时"
            echo "最后诊断信息:"
            echo "  systemd 状态: $(sudo systemctl status diting --no-pager -l || echo 'unknown')"
            echo "  最近日志: $(sudo journalctl -u diting -n 20 --no-pager || echo 'no logs')"
            exit 1
          EOF

      # Step 11: 回滚(仅在健康检查失败时)
      - name: Rollback on failure
        if: failure() && steps.health-check.outcome == 'failure'
        run: |
          ssh ${{ secrets.ALIYUN_SSH_USER }}@${{ secrets.ALIYUN_ECS_HOST }} << 'EOF'
            echo "⚠️  开始回滚..."

            # 恢复到上一个版本
            if [ -L /opt/diting/previous ]; then
              PREVIOUS_TARGET=$(readlink /opt/diting/previous)
              ln -sfn "$PREVIOUS_TARGET" /opt/diting/current
              sudo systemctl restart diting

              echo "✅ 已回滚到: $PREVIOUS_TARGET"
            else
              echo "❌ 无法回滚: previous 符号链接不存在"
              exit 1
            fi
          EOF

      # Step 12: 清理旧版本(保留最近 3 个,清理 7 天前的)
      - name: Cleanup old releases
        if: success()
        run: |
          ssh ${{ secrets.ALIYUN_SSH_USER }}@${{ secrets.ALIYUN_ECS_HOST }} << 'EOF'
            cd /opt/diting/releases

            # 保留最近 3 个版本
            KEEP_COUNT=3
            ls -t | tail -n +$((KEEP_COUNT + 1)) | xargs -r rm -rf

            # 清理 7 天前的版本
            find . -maxdepth 1 -type d -mtime +7 -exec rm -rf {} \;

            echo "✅ 清理完成"
          EOF

      # Step 13: 部署成功总结
      - name: Deployment summary
        if: success()
        run: |
          echo "## 🎉 部署成功!"
          echo ""
          echo "- **版本**: ${{ steps.create-release.outputs.RELEASE_ID }}"
          echo "- **提交**: ${{ github.sha }}"
          echo "- **作者**: ${{ github.actor }}"
          echo "- **分支**: ${{ github.ref_name }}"
          echo "- **时间**: $(date -u +"%Y-%m-%dT%H:%M:%SZ")"

      # Step 14: 创建失败通知 Issue
      - name: Create failure issue
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const deployUrl = `${context.payload.repository.html_url}/actions/runs/${context.runId}`;
            const body = `## ❌ 部署失败

            **详情**:
            - **版本**: ${{ steps.create-release.outputs.RELEASE_ID || '未创建' }}
            - **提交**: ${{ github.sha }}
            - **作者**: ${{ github.actor }}
            - **分支**: ${{ github.ref_name }}
            - **时间**: ${new Date().toISOString()}

            **日志**: [查看 Actions 日志](${deployUrl})

            **可能原因**:
            - SSH 连接失败
            - 依赖安装失败
            - 服务启动失败
            - 健康检查超时

            **后续步骤**:
            1. 查看 Actions 日志定位错误
            2. 检查 ECS 服务器状态
            3. 验证 GitHub Secrets 配置
            4. 如果已回滚,确认服务正常运行
            `;

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `[Deploy Failed] ${new Date().toISOString().split('T')[0]} - ${context.sha.substring(0, 7)}`,
              body: body,
              labels: ['deployment', 'bug']
            });

# 触发流程说明:
# 1. 代码合并到 master 分支
# 2. Test workflow 自动运行(2-4 分钟)
# 3. Test 成功 → 触发 Deploy workflow
# 4. Test 失败 → Deploy workflow 不会触发(安全保护)
#
# 预期结果:
# ✅ 成功: 新版本部署并通过健康检查,旧版本已清理
# ❌ 失败: 自动回滚到上一个版本,服务保持可用,创建 Issue 通知
# ⏱️ 预期时间: 7-14 分钟(Test 2-4min + Deploy 5-10min)
#
# 所需 GitHub Secrets:
# - ALIYUN_ECS_HOST: ECS 服务器 IP 或域名
# - ALIYUN_SSH_USER: SSH 用户名(建议: deploy)
# - ALIYUN_SSH_PRIVATE_KEY: SSH 私钥(ED25519 格式)
#
# 安全保证:
# - 只有测试通过的代码才会部署到生产环境
# - workflow_run 触发器确保 Test 和 Deploy 串行执行
# - if 条件检查确保 Test 状态为 success
